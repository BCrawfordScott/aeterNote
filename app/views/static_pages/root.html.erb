<script type = 'text/javascript'>
  <% if !!current_user %>
     window.currentUser = <%= render(
     'api/users/user.json.jbuilder',
     user: current_user
     ).html_safe %>
  <% end %>
  window.staticImages = {
    logoMain: '<%= asset_url("æterNote_logo_lightbulb.png") %>'
  }
</script>

<main id="root"></main>
